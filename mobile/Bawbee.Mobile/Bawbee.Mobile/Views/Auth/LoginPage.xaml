<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Bawbee.Mobile.Views.Auth.LoginPage"
             xmlns:viewModels="clr-namespace:Bawbee.Mobile.ViewModels.Auth"
             Title="Login">

    <ContentPage.BindingContext>
        <viewModels:LoginViewModel />
    </ContentPage.BindingContext>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Register_ToolbarItem_Clicked" />
    </ContentPage.ToolbarItems>

    <StackLayout Padding="20, 50">
        <Image Source="wallet.png" Margin="30"/>
        
        <Entry Text="{Binding Email}"
               Placeholder="E-mail" />

        <Entry Text="{Binding Password}"
               Placeholder="Password" 
               IsPassword="True"/>

        <Button Command="{Binding LoginCommand}"
                Text="Login" />
    </StackLayout>
</ContentPage>